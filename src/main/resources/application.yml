server:
  port: 8084
  http2:
    enabled: true
sas:
  enabled: false
spring:
  application:
    name: vendor-api-service
  profiles:
    include: dev
  main:
    allow-bean-definition-overriding: true
springdoc:
  api-docs:
    path: /${spring.application.name}/v3/api-docs
  swagger-ui:
    path: /${spring.application.name}/swagger-ui.html
    tryItOutEnabled: true
  paths-to-match: /**
#logging:
#  config: classpath:logback-spring.xml
# Micro-services list
grpc:
  server:
    port: 9094
  client:
    game-aggregator-operator-api-service:
#      address: 'discovery:///game-aggregator-operator-api-service'
      address: 'static://localhost:9095'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext

#    application-grpc:
#      address: 'discovery:///application-sample2'
#      enableKeepAlive: true
#      keepAliveWithoutCalls: true
#      negotiationType: plaintext