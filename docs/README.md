# ga-vendor-api-service — docs pointer

Vendor integration docs and codebase lessons for this service live in the **platform repo**
(`game-aggregator/game-aggregator-platform`), not here — a single sanctioned home per the platform `CLAUDE.md`
(no parallel ad-hoc docs):

- **Per-vendor contracts:** `docs/vendors/<vendor>/` — e.g. `docs/vendors/cq9/README.md`, `jili`, `spribe`.
- **Codebase lessons / gotchas:** `/.tasks/lessons.md`.
- **Documentation rules:** `docs/governance/documentation-rules.md`.

When adding docs, follow that convention in the platform repo and (if useful) update this pointer.
